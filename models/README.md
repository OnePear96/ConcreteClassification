# Saved models

Use `resnet18.pt`, `small_cnn.pt` and `lightgbm.txt` for inference. The JSON files contain neural-network training settings; the history CSVs provide learning curves.

Training saves new weights under `output/<run>/models/`. Set `MODEL_DIR` to that folder to load them. The three model weights are included in this repository.
